# Frontend (React) environment variables
# By default, the app calls the backend at http://localhost:3001.
# Override if your backend is hosted elsewhere.

REACT_APP_API_BASE=http://localhost:3001

# Alternate/legacy name (also supported by the API client)
REACT_APP_BACKEND_URL=http://localhost:3001
